#pragma once

// Configuration archive fragments are textually combined to retain their
// private ZIP parser, staging state, and filesystem ownership.
#define MACLOCK_CONFIGURATION_ARCHIVE_INTERNAL 1
